# Staging environment (EXAMPLE — safe to commit).
# Copy real values into your CI provider's *Environment secrets/variables* for
# the `staging` environment. Never commit a real .env file (see .gitignore).

APP_ENV=staging
APP_BASE_URL=https://staging.soccer-academy.example

# Service credentials belong in GitHub Environment *secrets*, not here.
# DATABASE_URL=
# API_KEY=
